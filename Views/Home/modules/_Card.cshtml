@using MailGenerator.ViewModels.Shared
@model Card

@{
    string ActionText = "", ActionUrl = "", Title = "", Subtitle = "", Color = "", Text = "", Image = "", Width = "", MaxWidth = "";
    
    string Font = "font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;";

    MaxWidth = !string.IsNullOrEmpty(Model.Width) ? String.Format("max-width:{0};", Model.Width) : "width:100%;";
    Image = !string.IsNullOrEmpty(Model.ImageUrl) ? String.Format("<img style='width: 100%; height: auto' width='{0}' height='auto' src='{1}'>", Model.Width, Model.ImageUrl) : null;
    Color = !string.IsNullOrEmpty(Model.Color) ? String.Format("color:{0};", Model.Color) : null;

}
<table cellpadding="0" cellspacing="0" border="0" style="width:100%;">
        <tr>
            <td style="padding: 10px;">
                <!-- @Model.Content; -->

        <!-- <table cellpadding="0" cellspacing="0" border="0" style="background-color: #ffffff; @MaxWidth; border:1px solid #e0e0e0; border-radius: 3px;">
            <tbody>
                @{
                    if(!string.IsNullOrEmpty(Model.ImageUrl)){
                        <tr>
                            <td style="text-align:center">
                                @Html.Raw(@Image)
                            </td>
                        </tr>
                    }
                    if(!string.IsNullOrEmpty(Model.Title)){
                        <tr>
                            <td style="padding: 10px 10px 0 10px">
                                <h3 style="@Font @Color margin:0; font-weight: 500;">@Html.Raw(Model.Title)</h3>
                            </td>
                        </tr>
                    }
                    if(!string.IsNullOrEmpty(Model.Subtitle)){
                        <tr>
                            <td style="padding: 0 10px 0 10px">
                                <span style="color:#666; margin:0">@Html.Raw(Model.Subtitle)</span>
                            </td>
                        </tr>
                    }
                    if(!string.IsNullOrEmpty(Model.Text)){
                        <tr>
                            <td style="padding: 10px;">
                                <span>@Html.Raw(Model.Text)</span>
                            </td>
                        </tr>
                    }
                    if(!string.IsNullOrEmpty(Model.ActionText)){
                        <tr>
                            <td style="text-align: right; padding: 10px">
                                <a style="@Font color: #0071c5; text-transform:uppercase; text-decoration: none; font-weight: 500" href="@Model.ActionUrl">@Model.ActionText</a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table> -->
           </td>
    </tr>
</table>
                
